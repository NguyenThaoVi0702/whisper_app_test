training_args = Seq2SeqTrainingArguments(
    output_dir=NEW_ADAPTER_SAVE_PATH,
    per_device_train_batch_size=8,
    gradient_accumulation_steps=1,
    learning_rate=1e-5,
    warmup_steps=50,
    max_steps=1000,
    fp16=True,
    
    # --- The Fix ---
    do_eval=True,               # <<< ADD THIS LINE: Explicitly enable evaluation
    eval_steps=200,             # Tell it to evaluate every 200 steps
    save_steps=200,             # Tell it to save every 200 steps (now they match!)
    # --- End of Fix ---

    logging_steps=25,
    report_to=["tensorboard"],
    load_best_model_at_end=True,
    greater_is_better=False,
    remove_unused_columns=False,
    label_names=["labels"],
)
